# NeuralNetwork
ニューラルネットワークを理解するための記録としてのリポジトリ。
## XOR.py
XORを出力するだけのもの。  
活性化関数は隠れ層出力層ともに標準ロジスティック関数、損失関数は交差エントロピー誤差を使っており、エポック数は10000（ただし、途中で損失関数の出力が0.01未満になった場合は早期終了）。  
隠れ層のノード数は可変にしてあるが、デフォルトはn=4。理論上はn=2でもAND(OR,NAND)で出力できるが、鞍点や局所解でハマってしまったため少し増やした
